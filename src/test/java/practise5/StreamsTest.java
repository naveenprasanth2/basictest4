package practise5;

import java.util.List;
import java.util.Map;
import java.util.concurrent.ThreadLocalRandom;
import java.util.function.Function;
import java.util.function.Supplier;
import java.util.stream.Collectors;
import java.util.stream.IntStream;
import java.util.stream.Stream;

public class StreamsTest {
    public static void main(String[] args) {
//        IntStream.rangeClosed(0, 50).filter(x -> x % 2 == 0).findFirst().ifPresent(System.out::println);
//        IntStream.generate(() -> ThreadLocalRandom.current().nextInt(0, 10)).limit(10).forEach(System.out::println);
//        IntStream.iterate(0, x -> x + 1).limit(5).forEach(System.out::println);
//        Supplier<IntStream> test = () -> IntStream.rangeClosed(0, 10);
//        test.get().forEach(System.out::println);
//        Supplier<Stream<String>> test1 = () -> Stream.of("naveen".split(""));
//        test1.get().forEach(System.out::println);

//        IntStream.generate(() -> ThreadLocalRandom.current().nextInt(0,20))
//                                        .limit(10).forEach(System.out::println);


    }

}